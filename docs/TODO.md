- [ ] Creating a chat on a cloud machine (for example from homelab to macbook) causes t3r to reindex that chat since it already exists locally, then t3r reappears it in the UI. Investigate and prevent duplicate reindex/display behavior. (Symptom: on mobile, after a chat message goes through and a response completes, I get the sent message and the response message twice. Maybe because t3r is reindexing chats as I update them from my phone/server.)
- [ ] Workspace `codex-remote` is ~1.0 GB before compression during packaging and fails upload with `413 Request Entity Too Large` from nginx (`Uploading 277.0 MB...`); investigate and reduce upload payload or server limits for remote packaging path.
